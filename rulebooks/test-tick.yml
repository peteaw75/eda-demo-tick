---
- name: Test run job template
  hosts: all
  sources:
    - ansible.eda.tick:
        delay: 60

  rules:
    - name: Run ansible job
      condition: event.i is defined
      actions:
      - run_job_template:
          name: "EDA-test-tick"
          organization: "Default"
